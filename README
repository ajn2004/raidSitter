This project is to build a discord bot that track's a discord's channel.
  When implemented correctly, we should be able to specify
    which channel to watch
    which member to track
    parse the log systematically
    report analytics of aggregate log
